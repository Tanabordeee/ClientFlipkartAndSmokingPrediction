import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["www.channelengine.com",'img5.pic.in.th','img2.pic.in.th' , 'storage.googleapis.com','pic.in.th'],
  },
};

export default nextConfig;
